services:
  app:
    build:
      context: .
    image: host
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1450
