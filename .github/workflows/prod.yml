name: Deploy to Production VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  VPS_HOST: ${{ secrets.VPS_HOST }}
  VPS_USER: ${{ secrets.VPS_USER }}
  PROJECT_DIR: ${{ secrets.PROJECT_DIR }}

jobs:
  deploy:
    name: Deploy to nkolosov.com
    runs-on: ubuntu-latest
    timeout-minutes: 60

    environment:
      name: production
      url: https://nkolosov.com

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          echo "🔐 Setting up SSH key..."

          # Создаем директорию .ssh
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh

          # Проверяем наличие ключа в секретах
          if [ -z "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "❌ SSH_PRIVATE_KEY secret is not set!"
            exit 1
          fi

          # Записываем ключ в файл (пробуем оба способа)
          if [[ "${{ secrets.SSH_PRIVATE_KEY }}" == *"BEGIN"* ]]; then
            echo "📝 Using direct key format"
            echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          else
            echo "📝 Trying base64 decode"
            echo "${{ secrets.SSH_PRIVATE_KEY }}" | base64 -d > ~/.ssh/id_rsa
          fi
          chmod 600 ~/.ssh/id_rsa

          # Проверяем формат ключа
          if ! head -1 ~/.ssh/id_rsa | grep -q "BEGIN.*PRIVATE KEY"; then
            echo "❌ Invalid SSH key format!"
            echo "Key should start with -----BEGIN and contain PRIVATE KEY"
            exit 1
          fi

          # Запускаем ssh-agent
          eval $(ssh-agent -s)

          # Экспортируем переменные для последующих шагов
          echo "SSH_AUTH_SOCK=$SSH_AUTH_SOCK" >> $GITHUB_ENV
          echo "SSH_AGENT_PID=$SSH_AGENT_PID" >> $GITHUB_ENV

          # Добавляем ключ с детальной диагностикой
          echo "🔑 Adding SSH key to agent..."
          if ssh-add ~/.ssh/id_rsa; then
            echo "✅ SSH key added successfully"
            ssh-add -l
          else
            echo "❌ Failed to add SSH key to agent"
            echo "Key file size: $(wc -c < ~/.ssh/id_rsa) bytes"
            echo "Key file head:"
            head -3 ~/.ssh/id_rsa
            echo "Key file tail:"
            tail -3 ~/.ssh/id_rsa
            exit 1
          fi

      - name: Add VPS to known hosts
        run: |
          ssh-keyscan -H ${{ env.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Test SSH connection
        run: |
          echo "🔐 Testing SSH connection to VPS..."
          ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=no ${{ env.VPS_USER }}@${{ env.VPS_HOST }} "echo '✅ SSH connection successful!' && whoami && pwd"

      - name: Create deployment package
        run: |
          echo "📦 Creating deployment package..."
          echo "Current directory: $(pwd)"
          echo "Directory contents:"
          ls -la

          # Создаем временную директорию для чистой копии
          mkdir -p /tmp/portfolio-build

          # Копируем только нужные файлы, исключая ненужные
          echo "📋 Copying files to temporary directory..."
          if command -v rsync >/dev/null 2>&1; then
            rsync -av \
              --exclude='.git' \
              --exclude='node_modules' \
              --exclude='.next' \
              --exclude='logs' \
              --exclude='*.log' \
              --exclude='.env*' \
              --exclude='coverage' \
              --exclude='.nyc_output' \
              --exclude='cypress/videos' \
              --exclude='cypress/screenshots' \
              --exclude='.cache' \
              --exclude='dist' \
              --exclude='build' \
              --exclude='.DS_Store' \
              --exclude='*.tmp' \
              --exclude='*.temp' \
              --exclude='.github' \
              --exclude='.husky' \
              ./ /tmp/portfolio-build/
          else
            # Fallback: используем cp с find для исключения файлов
            echo "rsync not available, using cp with find..."
            find . -type f \
              ! -path './.git/*' \
              ! -path './node_modules/*' \
              ! -path './.next/*' \
              ! -path './logs/*' \
              ! -name '*.log' \
              ! -name '.env*' \
              ! -path './coverage/*' \
              ! -path './.nyc_output/*' \
              ! -path './cypress/videos/*' \
              ! -path './cypress/screenshots/*' \
              ! -path './.cache/*' \
              ! -path './dist/*' \
              ! -path './build/*' \
              ! -name '.DS_Store' \
              ! -name '*.tmp' \
              ! -name '*.temp' \
              ! -path './.github/*' \
              ! -path './.husky/*' \
              -exec cp --parents {} /tmp/portfolio-build/ \;
          fi

          # Создаем архив из временной директории
          echo "🗜️ Creating archive from clean copy..."
          cd /tmp
          tar -czf portfolio-deploy.tar.gz -C portfolio-build .

          # Перемещаем архив обратно в рабочую директорию
          mv portfolio-deploy.tar.gz $GITHUB_WORKSPACE/
          cd $GITHUB_WORKSPACE

          # Проверяем создание архива
          if [ -f "portfolio-deploy.tar.gz" ]; then
            echo "✅ Archive created successfully"
            echo "Archive size: $(du -h portfolio-deploy.tar.gz | cut -f1)"
            echo "Archive contents (first 20 files):"
            tar -tzf portfolio-deploy.tar.gz | head -20
          else
            echo "❌ Failed to create archive"
            exit 1
          fi

          # Очистка временной директории
          rm -rf /tmp/portfolio-build

      - name: Upload deployment package
        run: |
          echo "📤 Uploading deployment package to VPS..."
          echo "Target: ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:/tmp/"

          # Проверяем, что файл существует
          if [ ! -f "portfolio-deploy.tar.gz" ]; then
            echo "❌ Deployment package not found!"
            exit 1
          fi

          echo "📊 Package info:"
          ls -lh portfolio-deploy.tar.gz

          # Загружаем файл на сервер с подробным выводом
          echo "🔄 Starting upload..."
          scp -v -o ConnectTimeout=30 -o StrictHostKeyChecking=no portfolio-deploy.tar.gz ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:/tmp/

          # Проверяем успешность загрузки
          ssh -o StrictHostKeyChecking=no ${{ env.VPS_USER }}@${{ env.VPS_HOST }} "ls -lh /tmp/portfolio-deploy.tar.gz"

          echo "✅ Package uploaded successfully"

      - name: Deploy application
        timeout-minutes: 45
        run: |
          ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -o ConnectTimeout=10 ${{ env.VPS_USER }}@${{ env.VPS_HOST }} << 'EOF'
            set -e
            
            # Настройка Docker для более надежной работы в CI/CD
            export DOCKER_BUILDKIT=1
            export COMPOSE_DOCKER_CLI_BUILD=1
            export BUILDKIT_PROGRESS=plain
            
            echo "🚀 Starting deployment to nkolosov.com..."
            echo "Current user: $(whoami)"
            echo "Current directory: $(pwd)"
            
            # Проверяем и устанавливаем Docker если нужно
            if ! command -v docker >/dev/null 2>&1; then
              echo "🐳 Docker не найден, устанавливаем..."
              
              # Устанавливаем Docker напрямую
              sudo apt update
              sudo apt install -y ca-certificates curl gnupg lsb-release
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
              echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
              sudo apt update
              sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
              sudo systemctl start docker
              sudo systemctl enable docker
              echo "Docker установлен: $(docker --version)"
            else
              echo "✅ Docker уже установлен: $(docker --version)"
            fi
            
            # Проверяем наличие загруженного файла
            if [ ! -f "/tmp/portfolio-deploy.tar.gz" ]; then
              echo "❌ Deployment package not found in /tmp/"
              exit 1
            fi
            
            echo "📦 Package size: $(du -h /tmp/portfolio-deploy.tar.gz | cut -f1)"
            
            # Создаем директорию проекта если не существует
            mkdir -p /home/portfolio
            cd /home/portfolio
            
            echo "📂 Current location: $(pwd)"
            
            # Создаем бэкап текущей версии
            if [ -d "current" ]; then
              echo "📦 Creating backup of current version..."
              if [ -f "./current/scripts/backup.sh" ]; then
                echo "🗄️ Attempting database backup..."
                sudo ./current/scripts/backup.sh database 2>/dev/null || echo "⚠️  Database backup failed (this is normal for first deployment)"
              else
                echo "ℹ️  No backup script found in current version"
              fi
              
              echo "📁 Moving current version to backup..."
              mv current backup-$(date +%Y%m%d_%H%M%S) 2>/dev/null || true
              echo "✅ Backup created"
            else
              echo "ℹ️  No existing version to backup (first deployment)"
            fi
            
            # Распаковываем новую версию
            echo "📂 Extracting new version..."
            mkdir -p current
            cd current
            tar -xzf /tmp/portfolio-deploy.tar.gz
            
            echo "📋 Extracted files:"
            ls -la | head -10
            
            # Создаем .env файл для продакшена
            if [ ! -f ".env" ]; then
              if [ -f "../.env" ]; then
                echo "📋 Using existing .env file"
                cp ../.env .
              elif [ -f "env.example" ]; then
                echo "🔧 Creating production .env from template..."
                
                # Генерируем безопасные пароли и ключи (только буквы и цифры)
                DB_PASSWORD=$(openssl rand -hex 16)
                NEXTAUTH_SECRET=$(openssl rand -hex 32) 
                ENCRYPTION_KEY=$(openssl rand -hex 16)
                
                echo "📝 Generated secure credentials"
                echo "DB Password: $DB_PASSWORD (length: ${#DB_PASSWORD})"
                echo "NextAuth Secret: ${NEXTAUTH_SECRET:0:10}... (length: ${#NEXTAUTH_SECRET})"
                echo "Encryption Key: ${ENCRYPTION_KEY:0:10}... (length: ${#ENCRYPTION_KEY})"
                
                # Создаем .env файл более безопасным способом
                echo "🔄 Processing env.example..."
                cp env.example .env.tmp
                
                # Используем более простые замены по одной с проверкой ошибок
                echo "🔄 Replacing database password..."
                if ! sed -i "s/portfolio_password/$DB_PASSWORD/g" .env.tmp; then
                  echo "❌ Failed to replace database password with sed"
                  exit 1
                fi
                
                echo "🔄 Replacing NextAuth secret..."
                if ! sed -i "s/your-super-secret-jwt-secret-key-here/$NEXTAUTH_SECRET/g" .env.tmp; then
                  echo "❌ Failed to replace NextAuth secret with sed"
                  exit 1
                fi
                
                echo "🔄 Replacing encryption key..."
                if ! sed -i "s/your-32-character-encryption-key/$ENCRYPTION_KEY/g" .env.tmp; then
                  echo "❌ Failed to replace encryption key with sed"
                  exit 1
                fi
                
                # Проверяем, что замены прошли успешно
                if grep -q "portfolio_password\|your-super-secret-jwt-secret-key-here\|your-32-character-encryption-key" .env.tmp; then
                  echo "⚠️ Some placeholders were not replaced, using alternative method..."
                  
                  # Альтернативный метод с awk
                  awk -v db_pass="$DB_PASSWORD" -v auth_secret="$NEXTAUTH_SECRET" -v enc_key="$ENCRYPTION_KEY" '
                  {
                    gsub(/portfolio_password/, db_pass)
                    gsub(/your-super-secret-jwt-secret-key-here/, auth_secret)
                    gsub(/your-32-character-encryption-key/, enc_key)
                    print
                  }' env.example > .env
                  rm .env.tmp
                else
                  # Перемещаем готовый файл
                  mv .env.tmp .env
                fi
                
                echo "✅ Production .env file created with secure passwords"
                echo "🔧 Don't forget to configure SMTP settings for email functionality"
                
                # Проверяем результат
                echo "📋 First 10 lines of .env:"
                head -10 .env
              else
                echo "❌ No .env template found!"
                exit 1
              fi
            else
              echo "✅ .env file already exists"
            fi
            
            # Проверяем наличие необходимых скриптов
            if [ ! -d "scripts" ]; then
              echo "❌ Scripts directory not found!"
              exit 1
            fi
            
            # Устанавливаем права
            echo "🔧 Setting permissions..."
            chmod +x scripts/*.sh
            
            # Создаем пользователя portfolio если не существует
            if ! id "portfolio" &>/dev/null; then
              echo "👤 Creating portfolio user..."
              sudo useradd -m -s /bin/bash portfolio || true
            fi
            
            # Добавляем в группу docker (после установки Docker)
            sudo usermod -aG docker portfolio || true
            
            # Убеждаемся что Docker daemon запущен и доступен
            sudo systemctl start docker
            sudo systemctl enable docker
            
            # Устанавливаем права на Docker socket если нужно
            sudo chmod 666 /var/run/docker.sock || true
            
            # Создаем Docker конфигурационную директорию для пользователя portfolio
            sudo mkdir -p /home/portfolio/.docker
            sudo chown -R portfolio:portfolio /home/portfolio/.docker
            sudo chmod 755 /home/portfolio/.docker
            
            # Устанавливаем права на директорию проекта
            sudo chown -R portfolio:portfolio . 2>/dev/null || true
            
            # Запуск диагностики Docker перед деплоем
            echo "🔍 Running Docker diagnostics..."
            if [ -f "./scripts/docker-diagnostics.sh" ]; then
              chmod +x ./scripts/docker-diagnostics.sh
              ./scripts/docker-diagnostics.sh config || echo "⚠️ Docker diagnostics completed with warnings"
            else
              echo "ℹ️ Docker diagnostics script not found, skipping..."
            fi
            
            # Настройка Docker daemon для более стабильной работы
            echo "🔧 Optimizing Docker daemon configuration..."
            sudo mkdir -p /etc/docker
            if [ -f "./docker/daemon.json" ]; then
              echo "📋 Using optimized daemon.json configuration"
              sudo cp ./docker/daemon.json /etc/docker/daemon.json
            else
              echo "📝 Creating basic daemon.json configuration"
              echo '{' | sudo tee /etc/docker/daemon.json > /dev/null
              echo '  "log-driver": "json-file",' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '  "log-opts": {"max-size": "10m", "max-file": "3"},' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '  "storage-driver": "overlay2",' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '  "max-concurrent-downloads": 3,' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '  "max-concurrent-uploads": 5,' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '  "registry-mirrors": ["https://mirror.gcr.io"]' | sudo tee -a /etc/docker/daemon.json > /dev/null
              echo '}' | sudo tee -a /etc/docker/daemon.json > /dev/null
            fi
            
            # Перезапускаем Docker для применения настроек
            sudo systemctl restart docker
            sleep 10
            
            # Проверяем Docker
            echo "🔍 Verifying Docker configuration..."
            docker info | grep -E "Storage Driver|Logging Driver" || true
            
            # Очистка Docker перед сборкой для освобождения места
            echo "🧹 Cleaning up Docker resources..."
            docker system prune -f --volumes || true
            
            # Запускаем деплой через Docker
            echo "🐳 Starting Docker deployment..."
            if [ -f "./scripts/deploy.sh" ]; then
              # Запускаем от имени пользователя portfolio с правильными переменными окружения
              sudo -u portfolio -i bash -c "cd /home/portfolio/current && export DOCKER_BUILDKIT=1 && export COMPOSE_DOCKER_CLI_BUILD=1 && export BUILDKIT_PROGRESS=plain && ./scripts/deploy.sh"
            else
              echo "❌ Deploy script not found!"
              exit 1
            fi
            
            # Очистка
            rm /tmp/portfolio-deploy.tar.gz
            
            echo "✅ Deployment completed successfully!"
          EOF

      - name: Health check
        run: |
          echo "🔍 Performing health check..."

          # Проверяем статус сервисов на сервере
          ssh -o StrictHostKeyChecking=no ${{ env.VPS_USER }}@${{ env.VPS_HOST }} << 'EOF'
            echo "📊 Checking Docker services status..."
            cd /home/portfolio/current
            docker compose ps || echo "Docker compose not running yet"
            
            echo "🔍 Checking running containers..."
            docker ps --format "table {{.Names}}\\t{{.Status}}\\t{{.Ports}}"
          EOF

          # Ждем запуска приложения
          echo "⏳ Waiting 60 seconds for application to start..."
          sleep 60

          # Проверяем доступность
          echo "🌐 Testing application endpoints..."
          for i in {1..15}; do
            # Проверяем HTTP сначала
            if curl -f -s --connect-timeout 10 http://nkolosov.com/health > /dev/null 2>&1; then
              echo "✅ HTTP health check passed!"
              break
            fi
            
            # Проверяем HTTPS
            if curl -f -s --connect-timeout 10 -k https://nkolosov.com/health > /dev/null 2>&1; then
              echo "✅ HTTPS health check passed!"
              break
            fi
            
            # Проверяем по IP
            if curl -f -s --connect-timeout 10 http://${{ env.VPS_HOST }}:3000/api/health > /dev/null 2>&1; then
              echo "✅ Direct IP health check passed!"
              break
            fi
            
            echo "⏳ Attempt $i/15 - application not ready yet..."
            if [ $i -eq 15 ]; then
              echo "❌ Health check failed after 15 attempts"
              
              # Диагностическая информация при ошибке
              echo "🔍 Diagnostic information:"
              ssh -o StrictHostKeyChecking=no ${{ env.VPS_USER }}@${{ env.VPS_HOST }} << 'DIAG_EOF'
                echo "=== Docker containers ==="
                docker ps -a
                echo "=== Application logs ==="
                cd /home/portfolio/current
                docker compose logs --tail=50 app || echo "No app logs available"
                echo "=== Nginx logs ==="
                docker compose logs --tail=20 nginx || echo "No nginx logs available"
              DIAG_EOF
              
              exit 1
            fi
            
            sleep 20
          done

          echo "🎉 Application is healthy and responding!"

      - name: Deployment summary
        if: always()
        run: |
          echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Target**: https://nkolosov.com" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
